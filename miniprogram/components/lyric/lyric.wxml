<scroll-view hidden="{{isLyricHidden}}" class="lyric-scroll" scroll-y="true" scroll-with-animation="true" scroll-top="{{scrollTop}}">
  <view class="lyric-panel">
    <block wx:for="{{lyricList}}" wx:key="item">
    <view class="lyric {{index==currentIndex?'hightlight-lyric':''}} ">{{item.lrc}}</view>
  </block>
  </view>
</scroll-view>
